<%= render :partial => 'layouts/leftside', :locals => { :tab_index => "finance"} %>

<ul style="list-style-type:none">
	<%= render @bills %>
</ul>

<!-- will_paginate default style -->
<%= will_paginate @bills, :class=>"flickr_pagination" %>